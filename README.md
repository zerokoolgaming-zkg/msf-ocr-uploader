# MSF Screenshot → Sheet Uploader (Fully Automatic)
- Upload screenshots → OCR runs → portraits matched → row pushed to Google Sheet automatically.
- Portraits are in `/portraits` with `manifest.json`.
- Apps Script URL is already embedded in `config.js`.
